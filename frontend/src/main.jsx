import React from 'react'
import ReactDOM from 'react-dom/client'
import App_final_fixed from './App_final_fixed.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App_final_fixed />
  </React.StrictMode>,
)
